/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "1yy2pnaejct8g5v3.public.blob.vercel-storage.com"
      },
    ],
  },
};

module.exports = nextConfig
